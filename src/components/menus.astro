---
import { MENUS } from "../config";
---

<div class="flex flex-row gap-4">
  {
    MENUS.map((item) => (
      <a href={item.link} target={item.target} class="h-full text-sm text-tx2 hover:text-tx1 transition-colors">
        {item.title}
      </a>
    ))
  }
</div>
